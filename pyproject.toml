[project]
name = "invest-in"
version = "0.1.0"
description = "超長期・買いのみの株式投資を支援するローカルウォッチリストアプリ"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
    { name = "goodnasubi", email = "goodnasubi@gmail.com" }
]
dependencies = [
    "yfinance>=0.2.40",
    "requests>=2.31.0",
    "anthropic>=0.40.0",
]

[tool.uv]
package = false
